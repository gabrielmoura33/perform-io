export interface ProviderInfo {
  id: string;
  favorites: number;
  reviews: number;
  average_review: number;
  bio: string;
  work_schedule: string;
  week_schedule: string;
  video_url: string;
}
